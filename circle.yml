machine:
  python:
    version: 3.5.0
dependencies:
  override:
    - pip install -r test-requirements.txt
    - npm install
    - npm install -g codecov
    - grunt
test:
  override:
    - py.test
    - grunt phantomjs
    - codecov
